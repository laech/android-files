apply plugin: 'android-library'

dependencies {
  compile project(':files-io-os')
  compile project(':files-logging')
  compile libs.auto_value
  compile libs.commons_io

  androidTestCompile project(':files-common-testing')
}
