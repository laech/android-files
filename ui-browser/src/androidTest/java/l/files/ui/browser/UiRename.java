package l.files.ui.browser;

import l.files.ui.browser.action.RenameFragment;

final class UiRename extends UiFileCreation<UiRename> {

    UiRename(UiFileActivity context) {
        super(context, RenameFragment.TAG);
    }

}
