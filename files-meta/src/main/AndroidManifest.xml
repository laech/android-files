<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="l.files.meta">

  <application android:allowBackup="true">
    <provider
        android:authorities="l.files.meta"
        android:name=".MetaProvider"
        android:exported="false"/>
  </application>
</manifest>
