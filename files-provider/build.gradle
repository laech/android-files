apply plugin: 'com.android.library'

dependencies {
    compile project(':files-common')
    compile project(':operations')
    compile project(':files-io-file-event')
    compile libs.commons_io
    compile libs.commons_lang
    compile libs.guava
    compile libs.tika
}
