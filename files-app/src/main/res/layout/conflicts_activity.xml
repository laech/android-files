<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

  <LinearLayout
      android:orientation="vertical"
      android:layout_width="match_parent"
      android:layout_height="wrap_content"
      android:padding="16dp">

    <TextView
        android:id="@+id/summary"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:layout_marginBottom="16dp"
        android:textAppearance="?android:attr/textAppearanceMedium"/>

    <Button
        style="@style/Theme.ListButton"
        android:id="@+id/keep"
        android:drawableStart="@drawable/ic_action_split"
        android:text="@string/keep_all"/>

    <Button
        style="@style/Theme.ListButton"
        android:id="@+id/replace"
        android:drawableStart="@drawable/ic_action_merge"/>

    <Button
        style="@style/Theme.ListButton"
        android:id="@+id/skip"
        android:drawableStart="@drawable/ic_action_undo"/>

    <Button
        style="@style/Theme.ListButton"
        android:id="@+id/choose"
        android:drawableStart="@drawable/ic_action_edit"
        android:text="@string/let_me_choose"/>

  </LinearLayout>
</ScrollView>
