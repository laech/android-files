<manifest
    package="l.files.ui.preview"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application>

        <provider
            android:name=".DecodePdf$PdfPreviewProvider"
            android:authorities="@string/authority_pdfpreview"
            android:exported="false"
            android:process=":pdfpreview"/>

    </application>

</manifest>
