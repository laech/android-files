apply plugin: 'com.android.library'

android {
  defaultConfig {
    ndk {
      moduleName 'os'
      ldLibs 'log'
    }
  }
}

dependencies {
  compile libs.commons_lang
  compile libs.auto_value

  androidTestCompile project(':testlib')
  androidTestCompile libs.guava
}
